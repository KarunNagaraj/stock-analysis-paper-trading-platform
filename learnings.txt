1) Url is generated manually in stockSearch.tsx using <Link key={stock.id} to={`/stocks/${stock.symbol}`}>
2) frontend Router in app.tsx sees that change and renders stockdetails
3) Strock details extracts that symbol from the url and renders that specific's symbols data by making an api call to backend 
// 1. Create the URL
<Link to={`/stocks/${stock.symbol}`} />

// 2. Define what URLs should show the component
<Route path="/stocks/:symbol" element={<StockDetails />} />

// 3. Read the dynamic part of the URL
const { symbol } = useParams();

                 StockSearch.tsx
                       │
              User types "rel"
                       │
                       ▼
              searchStocks("rel")
                       │
                       ▼
          GET /stocks/search?q=rel
                       │
                       ▼
                    Backend
                       │
                       ▼
              [RELIANCE, ...]
                       │
                       ▼
              User clicks RELIANCE
                       │
                       ▼
       <Link to="/stocks/RELIANCE">
                       │
                       ▼
              React Router
                       │
                       ▼
                StockDetails
                       │
                useParams()
                       │
                 "RELIANCE"
                       │
                       ▼
          getStockBySymbol("RELIANCE")
                       │
                       ▼
          GET /stocks/RELIANCE
                       │
                       ▼
                    Backend
                       │
                       ▼
             Stock + fundamentals
                       │
                       ▼
               Display details


WORKING OF CANDLESTICK CHART GENERATION:
User opens:

/stocks/RELIANCE
        │
        ▼
StockDetails renders
        │
        ▼
<HistoricalPriceChart symbol="RELIANCE" />
        │
        ▼
HistoricalPriceChart renders
        │
        ├── historicalPrices = []
        │
        ├── chartContainerRef.current = null
        │
        ▼
React puts <div> into DOM
        │
        ▼
useEffect([symbol])
        │
        ▼
getHistoricalPrices("RELIANCE")
        │
        ▼
GET /api/stocks/RELIANCE/history
        │
        ▼
Backend → MySQL
        │
        ▼
historical data returned
        │
        ▼
setHistoricalPrices(data)
        │
        ▼
React renders again
        │
        ▼
useEffect([historicalPrices])
        │
        ├── find chart <div>
        │
        ├── createChart(...)
        │
        ├── add CandlestickSeries
        │
        ├── normalizeCandlestickData(...)
        │
        ├── setData(...)
        │
        ├── fitContent()
        │
        ▼
Lightweight Charts renders candles
        │
        ▼
User sees interactive chart

STOCK DETAILS renders the historical price chart which is a smaller version which then renders stock price chart.
A button is there which links to a full screen chart, When the above link is clicked, it opens a new chart through stock chart 
Stock chart then renders stock price chart
// HistoricalPriceChart.tsx
<StockPriceChart historicalPrices={historicalPrices} height={300} />

// StockChart.tsx
<StockPriceChart historicalPrices={historicalPrices} height={600} />